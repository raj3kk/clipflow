package com.clipflow.agent;

/**
 * Manual build (aapt2, no Gradle) — BuildConfig yahan define hai.
 * build-apk.sh VERSION_CODE/VERSION_NAME se auto-update karta hai.
 */
public final class BuildConfig {
    public static final String VERSION_NAME = "0.1.0-p71";
    public static final int VERSION_CODE = 72;
    private BuildConfig() {}
}
