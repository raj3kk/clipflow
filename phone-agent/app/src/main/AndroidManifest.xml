<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- p35: CAMERA/RECORD_AUDIO HATA DIYA. Instagram upload file chooser
         (onShowFileChooser) se hota hai — camera/mic permission chahiye hi
         nahi. Pehle jaise: zero camera/mic prompts. WebView ke getUserMedia
         requests WebChromeClient me hamesha silently deny hote hain. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- In-app auto-update (round-6 Worker D): APK install ke liye zaroori.
         Android 8+ pe pehli baar Settings.ACTION_MANAGE_UNKNOWN_APP_SOURCES
         se one-time permission leni padti hai (Hinglish guide ke saath). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- FCM (p7): manual build AAR manifest merge nahi karta, isliye Firebase ke
         components yahan haath se declare hain (google-services plugin normally
         yehi merged manifest me dalta hai). -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <application
        android:name=".AgentApp"
        android:label="AutoClip"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:usesCleartextTraffic="false"
        android:allowBackup="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Force update blocking screen (round-6 Worker D): force_update=true
             release aane pe MainActivity isi pe bhejti hai. Back blocked. -->
        <activity
            android:name=".UpdateActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />

        <service
            android:name=".work.RunnerService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <service
            android:name=".work.WakeService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- NOTE: WorkManager ka androidx.startup auto-init HATA DIYA (2026-09-18).
             Kuch phones pe provider-init me hi app crash ho kar home screen pe
             wapas aa jata tha (Application.onCreate se pehle — crash screen bhi
             nahi dikhta tha). Ab Scheduler pehli scheduling pe lazy init karta
             hai, try/catch ke saath. -->

        <receiver
            android:name=".work.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Crash hone par kaaran dikhane wali screen -->
        <activity
            android:name=".CrashActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />

        <!-- FCM (p9): FirebaseInitProvider AUTO-INIT HATA DIYA (2026-09-18).
             p7 me ye provider add hua tha aur tab se kuch phones pe app
             khulte hi band ho jata tha (provider Application.onCreate se
             PEHLE chalta hai — crash screen bhi nahi dikhta tha; p6 me
             androidx.startup provider se wahi symptom tha). Ab Firebase
             AgentApp.onCreate me LAZY + try/catch me init hota hai —
             Firebase toote to bhi app launch hogi (FCM ke bina polling
             backup chalta rahega). Receiver + ComponentDiscoveryService
             sirf declarations hain, launch pe kuch nahi chalate. -->

        <receiver
            android:name="com.google.firebase.iid.FirebaseInstanceIdReceiver"
            android:exported="true"
            android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
                <category android:name="${applicationId}" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.google.firebase.components.ComponentDiscoveryService"
            android:exported="false">
            <meta-data
                android:name="com.google.firebase.components:com.google.firebase.messaging.FirebaseMessagingRegistrar"
                android:value="com.google.firebase.components.ComponentRegistrar" />
            <meta-data
                android:name="com.google.firebase.components:com.google.firebase.installations.FirebaseInstallationsRegistrar"
                android:value="com.google.firebase.components.ComponentRegistrar" />
            <meta-data
                android:name="com.google.firebase.components:com.google.firebase.datatransport.TransportRegistrar"
                android:value="com.google.firebase.components.ComponentRegistrar" />
        </service>

    </application>
</manifest>
